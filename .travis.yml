language: node_js
node_js:
  - "stable"
cache:
  directories:
    - source/node_modules
script:
  - npm --prefix ./source install
  - npm --prefix ./source test
  - npm --prefix ./source run build
on:
  branch: master